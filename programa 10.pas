Program Pzim ;
var
	valorA, valorB, valorC, multiplicacao, divisao : real;
	
Begin
   writeln ('Qual o Valor A');
   readln (valorA);
   writeln ('Qual o Valor B');
   readln (valorB);
   writeln ('Qual o Valor C');
   readln (valorC);
    multiplicacao:= (valorA*ValorC);
    divisao:=(valorA*ValorC) / 2;
    writeln ('A area do triangulo que tem A por base e C por altura é de  ', divisao); 
    multiplicacao:= (ValorC * ValorC) * Pi ;
		writeln ('A area do circulo de raio C é de  ', multiplicacao) ;  
		multiplicacao:= (ValorB * ValorB);
		writeln ('A area do quadrado que tem lado B é de  ', multiplicacao) ;  
		
   
   
   
End.